// Central entry type constants.
// Use these everywhere instead of raw strings like 'image', 'video', 'statement'.
// If Sanity schema ever renames a type, update it here once.

export const ENTRY_TYPES = {
  IMAGE: 'image',
  VIDEO: 'video',
  STATEMENT: 'statement',
}
